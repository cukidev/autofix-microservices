server:
  port: 8092

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/msrepairvehicle_db
    username: postgres
    password: admin
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
